% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tangent_space.R
\name{untangent_space}
\alias{untangent_space}
\title{Map tangent-space vectors back to SPD covariance matrices}
\usage{
untangent_space(V, ref = NULL)
}
\arguments{
\item{V}{A numeric matrix \code{[n x p(p+1)/2]} of tangent-space features, or
a single numeric vector. If \code{ref} is \code{NULL}, the reference stored
in \code{attr(V, "reference")} (as returned by \code{tangent_space}) is
used.}

\item{ref}{Optional reference SPD matrix \code{[p x p]}.}
}
\value{
A list of \code{[p x p]} SPD matrices.
}
\description{
Inverse of \code{\link{tangent_space}}: reconstructs SPD matrices from their
tangent-space feature vectors and a reference point.
}
\examples{
set.seed(1)
X <- array(rnorm(10 * 5 * 64), dim = c(10, 5, 64))
covs <- epoch_covariances(X)
V <- tangent_space(covs, mean_method = "logeuclid")
covs_rec <- untangent_space(V)
max(abs(covs_rec[[1]] - covs[[1]]))

}
\seealso{
\code{\link{tangent_space}}
}
