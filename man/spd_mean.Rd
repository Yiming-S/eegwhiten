% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/riemann.R
\name{spd_mean}
\alias{spd_mean}
\title{Mean of a set of SPD covariance matrices}
\usage{
spd_mean(
  covs,
  metric = c("riemann", "logeuclid", "euclid"),
  tol = 1e-06,
  max_iter = 50,
  eps = 1e-12
)
}
\arguments{
\item{covs}{A list of \code{[p x p]} SPD matrices, or a 3D array
\code{[n, p, p]}.}

\item{metric}{One of \code{"riemann"}, \code{"logeuclid"}, or
\code{"euclid"}.}

\item{tol}{Convergence tolerance for the Riemannian mean.}

\item{max_iter}{Maximum iterations for the Riemannian mean.}

\item{eps}{Numeric stability floor for eigenvalues.}
}
\value{
A \code{[p x p]} SPD matrix.
}
\description{
Computes the average of several symmetric positive-definite covariance
matrices under one of three geometries: the affine-invariant Riemannian
(geometric / Karcher) mean, the log-Euclidean mean, or the arithmetic
(Euclidean) mean.
}
\examples{
set.seed(1)
covs <- lapply(1:5, function(i) cov(matrix(rnorm(200 * 4), 200, 4)))
M <- spd_mean(covs, metric = "riemann")
dim(M)

}
\seealso{
\code{\link{riemann_distance}}, \code{\link{barycenter_whitener}},
  \code{\link{tangent_space}}

Other riemannian: 
\code{\link{epoch_covariances}()},
\code{\link{riemann_distance}()},
\code{\link{tangent_space}()},
\code{\link{untangent_space}()}
}
\concept{riemannian}
