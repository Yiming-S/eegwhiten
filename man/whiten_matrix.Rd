% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/whiten_matrix.R
\name{whiten_matrix}
\alias{whiten_matrix}
\title{One-shot whitening for a single EEG matrix}
\usage{
whiten_matrix(
  X,
  center = TRUE,
  method = c("SVD", "ZCA", "ZCA-cor", "PCA", "PCA-cor", "Cholesky")
)
}
\arguments{
\item{X}{Numeric matrix [n_trials x n_channels].}

\item{center}{Logical; whether to subtract the column means.}

\item{method}{Whitening method; one of \code{"SVD"}, \code{"ZCA"},
\code{"ZCA-cor"}, \code{"PCA"}, \code{"PCA-cor"}, \code{"Cholesky"}.}
}
\value{
A list with components:
  \item{Z}{Whitened data matrix.}
  \item{W}{Whitening matrix.}
  \item{center}{Centering vector used.}
  \item{method}{Method name.}
}
\description{
Convenience wrapper around \code{whiten_model()} that fits
a whitening model on \code{X} and immediately applies it to
\code{X}, returning both the whitened data and the underlying
model parameters.
}
\examples{
set.seed(42)
X <- matrix(rnorm(200 * 16), 200, 16)
colnames(X) <- paste0("Ch", seq_len(16))
res <- whiten_matrix(X, center = TRUE, method = "PCA")
str(res$Z)

}
