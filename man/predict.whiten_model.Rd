% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/whiten_core.R
\name{predict.whiten_model}
\alias{predict.whiten_model}
\title{Apply a whitening model to new EEG data}
\usage{
\method{predict}{whiten_model}(object, newdata, self_center = FALSE, ..., recenter = NULL)
}
\arguments{
\item{object}{A \code{whiten_model} object.}

\item{newdata}{Numeric matrix with the same number of columns
as the data used to fit the model.}

\item{self_center}{Logical; if \code{TRUE}, center \code{newdata} by its own
column means instead of the stored training mean. This is useful for
cross-session / cross-subject transfer where the test data has shifted
relative to the training distribution. Ignored when the model was fitted
with \code{center = FALSE}.}

\item{...}{Unused.}

\item{recenter}{Deprecated; use \code{self_center}.}
}
\value{
Whitened data matrix (possibly with reduced dimensions).
}
\description{
Apply a whitening model to new EEG data
}
\examples{
set.seed(1)
X <- matrix(rnorm(200 * 8), 200, 8)
m <- whiten_model(X, method = "ZCA", lambda = 0.1)
Z <- predict(m, X)
dim(Z)

}
\seealso{
\code{\link{whiten_model}}, \code{\link{unwhiten}}
}
