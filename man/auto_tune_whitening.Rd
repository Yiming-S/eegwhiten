% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/whiten_enhancements.R
\name{auto_tune_whitening}
\alias{auto_tune_whitening}
\title{Automatically Tune Whitening Hyperparameters}
\usage{
auto_tune_whitening(
  X,
  y = NULL,
  methods = c("SVD", "ZCA", "ZCA-cor", "PCA", "PCA-cor", "Cholesky"),
  n_comp_grid = NULL,
  var_threshold_grid = NULL,
  lambda_grid = list("auto", 0, 0.05, 0.1),
  lambda_method = c("oas", "lw"),
  cov_estimator_grid = "empirical",
  sample_weight = NULL,
  cv_folds = 5L,
  seed = NULL,
  scoring = c("auto", "unsupervised", "accuracy"),
  score_fn = NULL,
  tyler_tol = 1e-06,
  tyler_max_iter = 200,
  tyler_eps = 1e-06,
  eig_method = c("auto", "base", "rspectra"),
  fast = FALSE,
  sign_reference = NULL,
  top_n = 5L
)
}
\arguments{
\item{X}{Numeric matrix \code{[n_samples x n_features]}.}

\item{y}{Optional labels for supervised tuning.}

\item{methods}{Candidate whitening methods.}

\item{n_comp_grid}{Optional integer grid for \code{n_comp}.}

\item{var_threshold_grid}{Optional numeric grid in \code{(0, 1]} for
\code{var_threshold}.}

\item{lambda_grid}{Candidate shrinkage values. Can include numeric values
in \code{[0, 1]} and \code{"auto"}.}

\item{lambda_method}{Method used when \code{lambda = "auto"}.}

\item{cov_estimator_grid}{Candidate covariance estimators.}

\item{sample_weight}{Optional sample weights (used in training folds).}

\item{cv_folds}{Number of folds.}

\item{seed}{Optional random seed.}

\item{scoring}{Scoring mode: \code{"auto"}, \code{"unsupervised"}, or
\code{"accuracy"}.}

\item{score_fn}{Optional custom scoring function. For unsupervised tuning,
called as \code{score_fn(Z_valid)}. For supervised tuning, called as
\code{score_fn(Z_train, y_train, Z_valid, y_valid)}.}

\item{tyler_tol}{Convergence tolerance for Tyler covariance.}

\item{tyler_max_iter}{Maximum iterations for Tyler covariance.}

\item{tyler_eps}{Numerical stabilization floor for Tyler covariance.}

\item{eig_method}{Eigen solver backend.}

\item{fast}{Logical; fast mode for eigensolver.}

\item{sign_reference}{Optional reference vectors for sign stabilization.}

\item{top_n}{Number of top configurations retained in the summary table.}
}
\value{
A list of class \code{whiten_tune} containing the best model,
  best parameters, and cross-validation ranking.
}
\description{
Performs cross-validation across candidate whitening configurations and
returns the best model fit on full data.
}
\examples{
set.seed(42)
X <- matrix(rnorm(150 * 6), 150, 6)
tuned <- auto_tune_whitening(
  X,
  methods = c("PCA", "ZCA"),
  n_comp_grid = c(3),
  lambda_grid = list(0, 0.1),
  cv_folds = 3, seed = 1, top_n = 4
)
head(tuned$ranking)

}
\seealso{
\code{\link{whiten_model}}, \code{\link{report_whitening}}
}
