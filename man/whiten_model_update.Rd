% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/whiten_enhancements.R
\name{whiten_model_update}
\alias{whiten_model_update}
\title{Incrementally Update a Whitening Model}
\usage{
whiten_model_update(model, X_new, sample_weight = NULL)
}
\arguments{
\item{model}{A \code{whiten_model} object.}

\item{X_new}{Numeric matrix of new samples with
\code{ncol(X_new) = model$dim_in}.}

\item{sample_weight}{Optional non-negative sample weights for
\code{X_new}.}
}
\value{
Updated \code{whiten_model} object.
}
\description{
Updates a fitted \code{whiten_model} using only summary moments from
new observations, avoiding a full refit from all historical data.
Currently supports models fitted with
\code{cov_estimator = "empirical"}.
}
\examples{
set.seed(1)
X1 <- matrix(rnorm(200 * 8), 200, 8)
m <- whiten_model(X1, method = "ZCA", lambda = 0.1)
X2 <- matrix(rnorm(100 * 8), 100, 8)
m_updated <- whiten_model_update(m, X2)
m_updated$n_obs

}
\seealso{
\code{\link{whiten_model}}
}
