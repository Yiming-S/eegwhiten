% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/whiten_enhancements.R
\name{report_whitening}
\alias{report_whitening}
\title{One-Click Whitening Report}
\usage{
report_whitening(
  model,
  data = NULL,
  Z = NULL,
  tune_result = NULL,
  file = "whitening-report.md",
  digits = 4
)
}
\arguments{
\item{model}{A \code{whiten_model} or \code{whiten_tune} object.}

\item{data}{Optional original-space data matrix used to compute
diagnostics.}

\item{Z}{Optional whitened matrix. If missing and \code{data} is provided,
\code{predict(model, data)} is used.}

\item{tune_result}{Optional tuning result from
\code{auto_tune_whitening()}.}

\item{file}{Output markdown path. If \code{NULL}, returns report text
without writing to disk.}

\item{digits}{Numeric formatting precision.}
}
\value{
If \code{file} is non-NULL, invisibly returns the output path.
  Otherwise returns the markdown text.
}
\description{
Generates a markdown report with model settings and whitening diagnostics.
}
\examples{
set.seed(1)
X <- matrix(rnorm(200 * 6), 200, 6)
m <- whiten_model(X, method = "ZCA", lambda = 0.1)
txt <- report_whitening(m, data = X, file = NULL)
cat(txt)

}
\seealso{
\code{\link{whiten_model}}, \code{\link{auto_tune_whitening}}
}
