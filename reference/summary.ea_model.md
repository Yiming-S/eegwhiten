# Summarize an EA model

Summarize an EA model

## Usage

``` r
# S3 method for class 'ea_model'
summary(object, ...)
```

## Arguments

- object:

  An `ea_model` object.

- ...:

  Unused.

## Value

The input object invisibly (prints reference-covariance conditioning).
